#include "main.h"
/**
 * print_triangle:- imprime un triangulo
 *
 * @size: el tamano del triangulo
 **/
void print_triangle(int size)
{
	int i;
	int j:

		if (size == 0)
			putchar('\n');
		for (i = 0; i < size; i++)
			_putchar(43);
			for (j = 0; j < size; j++)
				putchar(35);
		_putchar('\n');

