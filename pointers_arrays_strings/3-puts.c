#include "main.h"
 /**
  * _puts - escribe un string en caracter
  *
  * @str: string de caracteres
  **/
void _puts(char *str)
{
	int i:

		for (i = 0; srt[i] != '\0'; i++)
			_putchaf(str[i]);
	_putchar('\n');
}
