#include <stdio.h>
/**
 * main - funcion principal
 * @argc: contador
 * @argv: posicion
 * Return: Devuelve 0
 **/
int main(int argc, char *argv[])
{
	argc = 0;
	if (argc > 0)
	printf("%s", argv[argc]);
	return (0);
}
