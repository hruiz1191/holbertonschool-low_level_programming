#include <stdio.h>
/**
 * main - funcion principal
 * @argc: contador
 * @argv: posicion
 * Return: Devuelve 0
 **/
int main(int argc, char **argv)
{
	if (argc > 0)
	printf("%s\n", argv[0]);
	return (0);
}
